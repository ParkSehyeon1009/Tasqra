# Tasqra 개발·협업 가이드

> 기준: **DocFlow 통합 기획안 v0.3**  
> 대상: 백엔드·프런트엔드·인프라 전 팀원  
> 목적: 기존 OCR 미니프로젝트에서 가져갈 좋은 구조와 Tasqra에서 새롭게 적용할 규칙을 한곳에 정리한다.

---

## 1. 가장 먼저 알아야 할 원칙

Tasqra의 모든 설계와 구현은 **DocFlow 통합 기획안 v0.3**을 최우선 기준으로 한다.

```text
v0.3 기획
  ↓
이 개발 가이드
  ↓
API·DB 계약
  ↓
구현 코드와 기존 코드 주석
```

규칙이 서로 충돌하면 위에 있는 기준을 따른다.

- 기존 OCR 미니프로젝트 코드는 재사용 가능한 자산이다.
- 기존 코드와 주석이 v0.3 또는 이 가이드와 충돌하면 새 기준을 따른다.
- 기존 구조를 그대로 복제하는 것이 아니라, 필요한 기능과 좋은 설계만 선택적으로 계승한다.
- 임시 구현이 최종 구조처럼 굳어지지 않도록 변경 이유와 후속 작업을 남긴다.

---

## 2. 제품과 데이터의 핵심 흐름

Tasqra는 단순 OCR 도구가 아니라 문서 기반 프로젝트 협업 도구다.

```text
프로젝트 생성
  → 문서 업로드
  → 비동기 문서 추출·OCR
  → OCR 요소와 텍스트 저장
  → 필요 시 사용자 검수
  → 확정 텍스트 생성
  → 요약·분류·액션아이템 분석
  → 사용자 승인
  → 프로젝트 태스크 생성
  → 출처 문서와 OCR 박스로 역추적
```

핵심 인과 관계:

```text
OCR 품질
  → 확정 텍스트 품질
  → 액션아이템 정확도
  → 태스크 자동화 신뢰도
```

### 기능 계층

1. **공통 기반**: 인증, 프로젝트 스코프, 비동기 큐, OCR 요소 저장
2. **차별점 2A**: 액션아이템 추출과 태스크 자동화
3. **차별점 2B**: OCR 박스 검수와 확정 텍스트
4. **확장 기능**: 대시보드, 검색, 일괄 처리, 리포트, 폴더 감시

현재 개발 범위가 어느 계층인지 먼저 확인하고, 다음 계층 기능을 섞어 구현하지 않는다.

---

## 3. 기존 프로젝트에서 계승하는 규칙

### 3.1 백엔드 계층 구조

```text
Router → Service → Repository → Database
```

| 계층 | 책임 | 하지 않는 일 |
|---|---|---|
| Router | 요청 검증, 인증·권한 Dependency 적용, 응답 변환 | 비즈니스 로직, 직접 DB 쿼리 |
| Service | 업무 흐름, 상태 전이, 트랜잭션 경계 | HTTP 응답 생성, 직접 SQL 작성 |
| Repository | 조회·저장·수정·삭제 쿼리 | 비즈니스 정책 판단 |
| Model | ORM 관계와 DB 컬럼 | API 응답 형식 정의 |
| Schema | API 요청·응답 계약 | DB 쿼리와 업무 흐름 |

세부 규칙:

- Router는 Service를 `Depends`로 주입받는다.
- Service는 Repository를 생성자로 주입받는다.
- Router에서 DB Session 또는 ORM Query를 직접 사용하지 않는다.
- Service는 Repository를 거치지 않고 엔티티를 조회하지 않는다.
- 여러 DB 변경은 Service에서 하나의 트랜잭션으로 묶는다.
- AI나 OCR 외부 작업을 기다리는 동안 DB 트랜잭션을 열어두지 않는다.

### 3.2 Protocol과 Registry

- 문서 추출기는 공통 `TextExtractor` 계약을 따른다.
- AI 분석기는 공통 `Analyzer` 계약을 따른다.
- AI 제공자는 `AIClientProtocol`을 따른다.
- 확장자 또는 분석기 종류와 구현체 연결은 Registry에서 관리한다.
- 새 구현체 추가 때문에 기존 Service에 조건문이 계속 늘어나지 않게 한다.

### 3.3 의존성 주입과 자원 관리

- 객체 조립은 `dependencies.py`에서 담당한다.
- PaddleOCR처럼 모델 로딩 비용이 큰 객체는 애플리케이션 단위로 재사용한다.
- OCR 엔진에 대한 동시 추론 제어를 유지한다.
- 실제 AI와 Fake AI 선택은 환경설정으로 제어한다.
- 개발 환경의 `USE_FAKE_AI` 기본값은 반드시 `true`로 유지한다.

### 3.4 API 규칙

- API 경로는 `/api` 접두사를 사용한다. 헬스체크는 `/health`를 사용한다.
- 요청·응답 JSON 필드는 `snake_case`로 통일한다.
- 프런트에서도 서버 응답을 임의로 camelCase로 바꾸지 않는다.
- ORM 객체를 그대로 반환하지 않고 Pydantic Schema로 변환한다.
- 목록 API는 공통 페이징 구조를 사용한다.
- 오류 응답은 공통 형식을 사용한다.

```json
{
  "code": "DOCUMENT_NOT_FOUND",
  "message": "문서를 찾을 수 없습니다.",
  "request_id": "..."
}
```

- 사용자에게 노출할 메시지와 서버 내부 로그를 구분한다.
- 예상 가능한 업무 오류는 공통 `BusinessError`와 `ErrorCode`로 표현한다.

### 3.5 기존 핵심 데이터 분리

다음 세 데이터는 하나의 테이블로 합치지 않는다.

```text
documents        원본 문서와 처리 상태
extracted_texts  추출·확정된 본문
analyses         요약·분류·액션아이템 분석 결과
```

- 문서 하나에는 추출 텍스트 하나가 연결된다.
- 문서 하나에는 여러 분석 결과가 시간순으로 누적될 수 있다.
- 분석 결과는 분석기별 형태가 다르므로 JSONB로 저장할 수 있다.
- 분석 결과에는 provider, model, prompt version을 저장한다.
- `tokens_in`, `tokens_out`, `latency_ms`는 유지한다.

### 3.6 업로드 안전 규칙

- 사용자 파일명을 실제 저장 파일명으로 사용하지 않는다.
- 실제 저장 파일명은 UUID 등 서버가 생성한 값을 사용한다.
- 허용 확장자, 파일 크기, 빈 파일 여부를 검증한다.
- Router의 검증만 신뢰하지 않고 Service에서도 핵심 제한을 검증한다.
- 추출이나 DB 저장 실패 시 고아 파일이 남지 않게 처리한다.
- ZIP 기반 DOCX/HWPX를 처리할 때 상위 경로 탈출을 허용하지 않는다.

---

## 4. 기존 규칙 중 변경하거나 폐기하는 것

### 4.1 `create_all()` 기반 DB 관리 폐기

기존 방식:

```text
애플리케이션 시작 시 Base.metadata.create_all()
```

Tasqra 방식:

```text
모든 DB 구조 변경은 Alembic 마이그레이션으로 관리
```

- 모델 변경과 마이그레이션 파일을 같은 작업 단위에 포함한다.
- 이미 공유된 마이그레이션 파일을 임의로 수정하지 않는다.
- 기존 마이그레이션을 되돌려 고치는 대신 새 마이그레이션을 추가한다.
- 운영 애플리케이션 시작 과정에서 테이블을 자동 생성하지 않는다.

### 4.2 요청 내부 동기 OCR·LLM 처리 폐기

기존 방식:

```text
업로드 요청 → OCR 완료까지 대기 → 응답
```

Tasqra 방식:

```text
업로드 요청 → 문서·작업 생성 → 큐 등록 → 즉시 응답
                                      ↓
                                 Worker 처리
```

- 오래 걸리는 OCR, 문서 추출, LLM 분석, 일괄 처리는 Worker에서 실행한다.
- 작업 상태와 진행률을 저장한다.
- 프런트에는 폴링 또는 SSE로 상태를 전달한다.
- 작업은 재실행돼도 중복 결과가 생기지 않도록 멱등성을 고려한다.

### 4.3 문서 ID 단독 접근 폐기

기존 방식:

```python
get_by_id(document_id)
```

Tasqra 방식:

```python
get_by_id(project_id, document_id)
```

- 모든 프로젝트 데이터 조회에는 프로젝트 스코프를 강제한다.
- URL의 ID만으로 다른 프로젝트 데이터에 접근할 수 없어야 한다.
- 프런트에서 버튼을 숨기는 것만으로 권한을 보장하지 않는다.

---

## 5. v0.3 기준 신규 규칙

### 5.1 프로젝트 스코프

- 모든 문서는 정확히 하나의 프로젝트에 소속된다.
- 모든 태스크는 정확히 하나의 프로젝트에 소속된다.
- 프로젝트 멤버만 해당 프로젝트 데이터에 접근할 수 있다.
- 프로젝트 소유자와 업로드 사용자를 기록한다.
- Repository 메서드는 가능한 한 `project_id`를 필수 인자로 받는다.

기본 관계:

```text
users ──< project_members >── projects
                                 │
                                 ├──< documents
                                 ├──< tasks
                                 └──< activity_logs
```

### 5.2 역할과 권한

| 역할 | 권한 |
|---|---|
| OWNER | 프로젝트 설정, 멤버 관리, 모든 편집 |
| EDITOR | 문서·OCR·태스크 생성 및 수정 |
| VIEWER | 조회만 가능 |

- 인증은 공통 `get_current_user` Dependency에서 처리한다.
- 프로젝트 권한은 공통 Policy 또는 Dependency에서 처리한다.
- Router마다 권한 조건을 복사해 작성하지 않는다.
- Service도 중요한 변경 작업에서 권한 또는 스코프가 전달됐는지 확인한다.

### 5.3 문서 처리 모드와 상태

문서 처리 모드:

```text
NORMAL  자동 추출 후 바로 분석
REVIEW  OCR 후 사용자 검수를 거쳐 확정 텍스트로 분석
```

`BATCH`는 문서 처리 모드가 아니라 여러 문서를 등록한 작업 단위로 관리한다.

처리 상태와 검수 상태는 분리한다.

```text
processing_status:
PENDING → EXTRACTING → EXTRACTED → ANALYZING → COMPLETED
                                         └→ FAILED

review_status:
NOT_REQUIRED
PENDING → IN_PROGRESS → COMPLETED
```

- 상태 전이는 Service 또는 Worker에서만 변경한다.
- 프런트가 임의의 상태 문자열을 만들어 보내지 않는다.
- 실패 상태에는 사용자용 사유와 내부 로그를 구분한다.

### 5.4 OCR 페이지와 요소

OCR 원천 데이터는 `ocr_elements`다.

```text
documents
  ├── document_pages
  │     └── ocr_elements
  └── extracted_texts
```

`document_pages`에 저장할 핵심 정보:

- 페이지 번호
- 좌표 기준 이미지 경로
- 너비와 높이
- 회전 정보
- PDF 렌더링 배율

`ocr_elements`에 저장할 핵심 정보:

- 페이지
- 최초 OCR 텍스트
- 현재 사용자 수정 텍스트
- confidence
- Bounding Box 좌표
- source
- 읽기 순서
- 그룹
- 검수 상태
- 수정 버전
- 논리 삭제 여부

좌표 규칙:

- DB에는 페이지 크기 대비 0~1 비율 좌표로 저장한다.
- 프런트와 백엔드는 동일한 페이지 이미지를 좌표 기준으로 사용한다.
- 페이지 번호는 **1부터 시작**하는 것을 기본 규칙으로 한다.
- 회전된 글자나 사각형으로 표현할 수 없는 경우를 위해 polygon 확장을 고려한다.

### 5.5 OCR 수정과 Revision

- `original_text`는 최초 OCR 결과이며 사용자 수정으로 덮어쓰지 않는다.
- `text`는 현재 화면과 확정 텍스트에 사용할 값이다.
- 동일한 박스를 수정할 때 박스 ID를 유지한다.
- 기본 삭제 방식은 논리 삭제다.
- OCR 요소 변경 시 문서의 `ocr_revision`을 증가시킨다.
- 동시 수정 충돌 방지를 위해 요소별 `version`을 사용한다.
- 박스 변경과 최종 텍스트 재조립은 하나의 업무 단위로 처리한다.

```text
ocr_elements 변경
  → documents.ocr_revision 증가
  → reading_order 기준 텍스트 재조립
  → extracted_texts.content 갱신
  → extracted_texts.text_version 갱신
```

### 5.6 확정 텍스트와 분석

- `extracted_texts.content`는 OCR 요소를 조립한 스냅샷이다.
- 검수 모드에서는 검수 완료된 텍스트만 확정본으로 취급한다.
- 분석 실행 시 사용한 `source_text_revision`을 기록한다.
- 현재 OCR revision과 분석 revision이 다르면 해당 분석은 오래된 결과다.
- 검수 완료 후 요약·분류·액션아이템 분석을 한 번 실행한다.
- 검수 완료 문서를 다시 수정하면 `IN_PROGRESS`로 되돌리고 재분석 필요 상태로 표시한다.

### 5.7 액션아이템과 태스크

- AI가 생성한 액션아이템을 자동으로 태스크에 확정 등록하지 않는다.
- AI 결과는 항상 사용자 승인 카드로 먼저 보여준다.
- 승인된 항목만 태스크가 된다.
- AI 생성 여부를 저장한다.
- 태스크는 출처 분석과 출처 OCR 요소를 참조할 수 있어야 한다.
- 출처를 선택하면 원본 페이지와 OCR 박스를 하이라이트한다.
- 출처 OCR 요소가 수정되거나 논리 삭제돼도 FK와 감사 이력이 깨지지 않게 한다.

### 5.8 비동기 작업

- Celery와 Redis를 공통 작업 큐로 사용한다.
- OCR, LLM, 일괄 처리 작업은 동일한 상태 모델을 따른다.
- Worker는 API Router나 Pydantic 응답 Schema에 의존하지 않는다.
- Worker 입력에는 필요한 ID만 전달하고 DB에서 최신 상태를 다시 조회한다.
- 재시도 가능 오류와 재시도 불가능 오류를 구분한다.
- AI나 OCR 호출 중 DB 트랜잭션을 유지하지 않는다.
- 성공 결과 저장은 가능한 한 짧은 트랜잭션으로 처리한다.

---

## 6. 폴더별 책임

### 백엔드

```text
backend/app/
├── api/routes/       HTTP 요청·응답과 Dependency 연결
├── services/         업무 흐름과 상태 전이
├── repositories/     DB 쿼리
├── models/           SQLAlchemy ORM 모델
├── schemas/          Pydantic 요청·응답 계약
├── extractors/       문서·OCR 추출
├── analyzers/        LLM 분석기
├── ai/               AI 제공자 구현
├── workers/          비동기 작업 진입점
├── core/             설정·오류·보안·로깅·트랜잭션
├── db/               DB 엔진·세션·Alembic 연결
├── dependencies.py   객체 조립
└── main.py           애플리케이션 진입점과 Router 등록
```

### 프런트엔드

```text
frontend/src/
├── api/              서버 API 호출
├── pages/            Route 단위 화면
├── components/       재사용 UI와 도메인 컴포넌트
├── hooks/            재사용 상태·비동기 로직
├── layouts/          공통 화면 레이아웃
├── styles/           디자인 토큰과 전역 스타일
└── utils/            React·API에 의존하지 않는 순수 함수
```

- Page에 모든 API·상태·표시 로직을 계속 누적하지 않는다.
- 여러 화면에서 쓰는 서버 호출은 `api/`에 둔다.
- 여러 컴포넌트에서 쓰는 상태 로직은 Hook으로 분리한다.
- 프런트에서 서버 데이터 구조를 임의로 재정의하지 않는다.

---

## 7. API·DB·상태 계약을 먼저 맞추는 방법

새 기능 구현 전에 담당자들이 다음 항목을 먼저 합의한다.

1. 테이블과 관계
2. Enum과 상태 전이
3. API 경로와 HTTP 메서드
4. 요청·응답 Schema
5. 인증과 역할별 권한
6. 오류 코드
7. 비동기 작업 입력·출력
8. OCR 좌표 기준

권장 작업 순서:

```text
계약 합의
  → Schema·Enum·Migration 작성
  → 백엔드·프런트·Worker 병렬 구현
  → 통합 확인
```

계약이 확정되기 전에 각 담당자가 같은 개념을 서로 다른 이름으로 구현하지 않는다.

---

## 8. 파일을 추가하거나 변경할 때 함께 확인할 것

### DB 모델 변경

- [ ] Alembic 마이그레이션을 추가했는가?
- [ ] FK 삭제 정책과 인덱스를 확인했는가?
- [ ] Repository와 응답 Schema를 함께 확인했는가?
- [ ] 프런트가 사용하는 필드에 영향이 없는가?

### 환경변수 추가

- [ ] `core/config.py`에 필드를 추가했는가?
- [ ] `.env.example`에 설명과 예시를 추가했는가?
- [ ] Docker Compose 또는 배포 환경에 전달했는가?
- [ ] 비밀값을 Git에 포함하지 않았는가?

### API 변경

- [ ] Router 경로와 HTTP 메서드가 합의됐는가?
- [ ] 요청·응답 Schema가 정의됐는가?
- [ ] 인증과 프로젝트 권한이 적용됐는가?
- [ ] 공통 오류 형식을 사용하는가?
- [ ] 프런트 API 클라이언트와 함께 확인했는가?

### 새 패키지 추가

- [ ] 실제로 필요한 패키지인가?
- [ ] 버전 호환성을 확인했는가?
- [ ] `requirements.txt` 또는 `package.json`에 반영했는가?
- [ ] Docker에 필요한 시스템 패키지가 있는가?
- [ ] 팀원이 동일하게 설치할 수 있는가?

### 새 Router·Service·Repository 추가

- [ ] 책임이 올바른 폴더에 있는가?
- [ ] `dependencies.py`에서 조립했는가?
- [ ] Router를 `main.py`에 등록했는가?
- [ ] Service가 Repository를 우회하지 않는가?
- [ ] 프로젝트 스코프를 강제하는가?

### OCR 변경

- [ ] 페이지와 좌표 기준이 유지되는가?
- [ ] OCR 박스 ID와 revision 정책을 지키는가?
- [ ] 사용자 수정본을 덮어쓰지 않는가?
- [ ] 최종 텍스트 재조립이 함께 실행되는가?
- [ ] 기존 분석이 오래된 결과가 되는지 판단하는가?

---

## 9. Docker와 환경설정 규칙

- 서비스명과 컨테이너명은 Tasqra 기준으로 통일한다.
- `.env.example`은 공유 가능한 예시 파일이다.
- 실제 실행에는 Git에서 제외된 `.env` 또는 배포 환경의 Secret을 사용한다.
- `.env.example`을 실제 비밀값 저장소로 사용하지 않는다.
- 개발용 `--reload`와 운영 실행 명령을 구분한다.
- 업로드 파일, 모델 캐시, DB 데이터는 목적에 맞는 볼륨으로 관리한다.
- 업로드 파일과 로컬 캐시는 Git에 포함하지 않는다.
- PaddleOCR 모델을 실행 시 다운로드할지 이미지에 포함할지 배포 환경별로 명시한다.

권장 서비스명:

```text
tasqra-db
tasqra-api
tasqra-frontend
tasqra-redis
tasqra-worker
```

---

## 10. Git과 협업 규칙

- 한 작업은 가능한 한 하나의 목적만 가진다.
- 대규모 리팩터링과 기능 추가를 같은 변경에 섞지 않는다.
- 다른 담당자의 파일을 수정해야 하면 변경 이유와 API 영향을 먼저 공유한다.
- 생성 파일, 업로드 파일, `.env`, 캐시, 모델 데이터는 커밋하지 않는다.
- DB 모델 변경은 마이그레이션 없이 완료로 보지 않는다.
- API 계약 변경은 백엔드와 프런트 담당자가 함께 확인한다.
- 임시 코드에는 제거 조건이나 후속 작업을 남긴다.
- 기존 사용자 변경을 임의로 되돌리지 않는다.

작업 완료 공유에는 다음을 포함한다.

```text
- 무엇을 변경했는지
- API 또는 DB 계약이 바뀌었는지
- 새 환경변수·패키지가 있는지
- 직접 확인한 항목
- 아직 확인하지 못한 항목
- 다음 담당자가 해야 할 일
```

---

## 11. 현재 코드의 전환 대상

현재 저장소는 기존 미니프로젝트 구조를 옮긴 초기 상태다. 아래 항목은 새 규칙으로 순차 전환해야 한다.

### 우선 전환

- [ ] Docker 서비스명과 DB명을 Tasqra로 변경
- [ ] 실제 `.env`와 `.env.example` 역할 분리
- [ ] SQLAlchemy 주요 패키지 버전 고정
- [ ] `create_all()` 제거 및 Alembic 도입
- [ ] 업로드 파일을 `.gitignore`에 추가
- [ ] 백엔드 Docker 빌드와 `/health` 실행 확인
- [ ] 이미지·PDF·DOCX·HWPX 추출 확인

### 공통 기반

- [ ] 사용자 인증과 `get_current_user`
- [ ] 프로젝트·멤버·역할 모델
- [ ] 모든 Repository에 프로젝트 스코프 적용
- [ ] Celery·Redis Worker 구성
- [ ] 문서 처리 상태와 진행률 제공
- [ ] `document_pages`와 `ocr_elements` 영속화

### 차별 기능

- [ ] 액션아이템 분석기와 승인 카드
- [ ] 프로젝트 태스크와 칸반
- [ ] 태스크 출처 역추적
- [ ] OCR 박스 조회·수정·재OCR
- [ ] 검수 완료와 확정 텍스트
- [ ] OCR revision과 분석 revision 연결

---

## 12. 하지 않을 것

현재 제품 범위에서 다음 기능은 구현하지 않는다.

- 실시간 공동 편집
- 간트 차트와 공수·비용 관리
- 모바일 네이티브 애플리케이션
- 사용자 승인 없는 AI 태스크 자동 확정
- 프로젝트 스코프를 우회하는 전역 문서 조회
- OCR 원문과 사용자 수정본을 구분하지 않는 덮어쓰기

확장 기능은 공통 기반과 현재 마일스톤이 안정화된 후 진행한다.

---

## 13. 빠른 판단 기준

구현 방향이 애매할 때 다음 질문을 순서대로 확인한다.

1. v0.3 제품 흐름에 필요한가?
2. 현재 마일스톤 범위인가?
3. 프로젝트 스코프와 권한을 지키는가?
4. Router·Service·Repository 책임을 지키는가?
5. OCR 원본·수정본·revision을 보존하는가?
6. 오래 걸리는 작업을 요청 안에서 실행하고 있지 않은가?
7. API·DB 계약이 팀과 합의됐는가?
8. 다른 담당자가 같은 환경에서 재현할 수 있는가?

하나라도 명확하지 않다면 구현을 크게 진행하기 전에 팀과 계약부터 맞춘다.
